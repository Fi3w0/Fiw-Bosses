# Build output
.gradle/
build/
out/
bin/
*.class

# Loom / Forge / NeoForge dev runs + caches
run/
run-server/
runs/
logs/
.fabric/
*.log

# Root/module dev runtime folders. Do not use **/client or **/server here:
# source packages may legitimately be named client/server.
/run/
/*/run/
/client/
/*/client/
/server/
/*/server/

# Packaged release jars (publish via GitHub Releases, not in git)
/jars/
/Jar/

# Keep the Gradle wrapper jar tracked even if jar rules get added later
!gradle/wrapper/gradle-wrapper.jar

# IDE / editors
.idea/
.vscode/
.eclipse/
.settings/
*.iml
*.ipr
*.iws
.project
.classpath

# OS cruft
.DS_Store
Thumbs.db

# AI agent tooling / instructions (kept out of the published repo)
.claude/
AGENTS.md
CLAUDE.md
.cursor/
.cursorrules
.github/copilot-instructions.md

# Runtime config/data generated on a live server (never commit real ones).
# Anchored to repo root so the core 'config' SOURCE package is NOT ignored.
/config/
/run*/config/
**/profiles/
whitelist.json
